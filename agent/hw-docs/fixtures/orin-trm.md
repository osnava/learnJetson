# orin-trm (TRM slab mode)

TRM rendering — converted in 250-page slabs, md only: the docling JSON
source of truth is deliberately not persisted for this document (a full
one would be ~970 MB). Fixture stands in for any fetched doc without a
JSON beside it.
